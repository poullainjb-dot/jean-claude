# Phase 1: transactions data model + manual CSV import
python-dotenv>=1.0
pytest>=7.0

# Phase 3: dashboard
streamlit>=1.38
pandas>=2.0

# Later phases will add: yfinance, pytr, exchange SDKs, anthropic
